// permite trabajar sobre un array y saber si esta ahi un elemento

let numbers = [1, 2, 3, 4, 8, 9, 7]

console.log(numbers.includes(4));

const list = [ 'Mario', 'Angel', 'Heyyooo']

console.log(list.includes('mario'));
// Debe de pasar el mismo dato, igual como esta escrito, igual si hay un numero dentro de un string, se deben hacer las conversion de tipo para poder hacer la comparacion